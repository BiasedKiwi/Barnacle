services:
  barnacle:
    build: .
    depends_on:
      - redis
    links:
      - redis
  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"
networks:
  default:
    external:
      name: "barnacle"
    driver: bridge
